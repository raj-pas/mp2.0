# Baseline Metrics — HEAD `f5f2519` (2026-05-02)

Pre-Phase-1 baseline. Per-phase exit pings diff against this so silent
regression (test removals, coverage drops, runtime drift) surfaces
immediately.

## Test counts

**Total: 362 passing.**

Per-suite breakdown (from `pytest --collect-only -q`):

| Suite | Tests |
|---|---|
| `engine/tests/*` | 241 (computed) |
| `web/api/tests/test_append_only_invariants.py` | 5 |
| `web/api/tests/test_auth_boundaries.py` | 7 |
| `web/api/tests/test_db_state_integrity.py` | 9 |
| `web/api/tests/test_extraction_pipeline.py` | 21 |
| `web/api/tests/test_r1_audit_emission.py` | 10 |
| `web/api/tests/test_r1_preview_endpoints.py` | 23 |
| `web/api/tests/test_review_ingestion.py` | 42 |
| `web/api/tests/test_settings_contract.py` | 2 |
| `web/audit/tests/test_writer.py` | 2 |
| **web/audit total** | **121** |

(Engine 241 = 362 total − 121 web/audit; verify on first re-run.)

## Gate-suite timings (approximate)

| Gate | Wall-clock |
|---|---|
| `pytest engine + web + audit` | ~38s |
| `ruff check .` | <2s |
| `ruff format --check .` | <2s |
| `npm run typecheck` | ~10s |
| `npm run lint` | ~10s |
| `scripts/check-vocab.sh` | <1s |
| `makemigrations --check --dry-run` | ~3s |

Total local gate suite: ~1-2 minutes.

## Frontend metrics

| Metric | Value |
|---|---|
| Foundation Playwright e2e | 13/13 passing |
| Real-browser smoke | 1/1 passing (clean console) |
| TS strict + noUncheckedIndexedAccess | clean |
| ESLint --max-warnings=0 | clean |

(Bundle-size capture deferred per user 2026-05-02; defer until scale.)

## DB state at baseline

Per `docs/agent/post-r7-handoff-2026-05-01.md` §3 (re-confirmed
2026-05-02):

- Sandra/Mike Chen — synthetic, 1 household, fresh seed.
- `Seltzer review (demo prep)` — real_derived workspace, 5/5 reconciled.
- `Weryha review (demo prep)` — real_derived workspace, 5/5 reconciled.
- 1 incidental R7 e2e leftover (cleared by `reset-v2-dev.sh --yes`).
- Local advisor: `advisor@example.com`. Analyst: `analyst@example.com`.

DB integrity invariants: 9/9 passing on the freshly-restored demo state
(per `web/api/tests/test_db_state_integrity.py`).

## Niesner real-PII reference (historical, not in current DB)

Niesner workspace `d689fe68-c335-44ae-bbb3-104974b7e764` was created
in the 2026-05-01 session and reset out by R10c restoration. The
reference fact summary (per dossier §6 + handoff-log post-R7 entry):

- 12 docs uploaded
- 12/12 reconciled (post-`52e3327` max_tokens fix; was 10/12 pre-fix)
- 493 facts extracted
- 25 cross-source conflicts surfaced

Phase 7.2 (Niesner regression) re-uploads this folder; expectation is
12/12 reconciled at HEAD with new tool-use prompts (Phase 4).

## R10 sweep (historical)

55/55 reconciled across 7 client folders, 2,304 facts (per
`docs/agent/handoff-log.md` 2026-05-01 R10c entry). Phase 7.3 re-runs
this sweep.

## CI status

`.github/workflows/smoke.yml` runs on `push: branches: [main]` and
`pull_request:`. **Does NOT run on pushes to `feature/ux-rebuild`.**
CI signal arrives when the user opens a PR. Local gate suite is the
real signal during this session.

CI gate parity vs locked operational protocol:
- CI runs: ruff check + format, mypy strict (engine modules),
  pytest, makemigrations, spectacular validate, vocab,
  typecheck, lint, prettier, build, browser-e2e (synthetic).
- Local-only gates added in this session: PII grep guard
  (Phase 2), OpenAPI codegen drift (Phase 4.5), pytest-benchmark
  perf gate (Phase 6.9), 100% coverage (Phase 6.4),
  Vitest unit (Phase 6.6), axe-core a11y (Phase 5b.14),
  bandit + eslint-plugin-security (Phase 0.9), real-browser-smoke
  + pilot-features-smoke Playwright specs.
- Phase 8 should optionally extend `.github/workflows/smoke.yml`
  with the new gates if user wants CI parity, OR keep CI minimal
  (current scope) and rely on local pre-push.

## Pre-existing dev-deps

From `pyproject.toml [dependency-groups].dev`:
- hypothesis (already installed; Phase 6.3 uses it)
- mypy (already installed; CI uses strict on engine)
- pytest, pytest-django, ruff, scipy

Phase 6 + Phase 0.9 add: bandit, pytest-cov, pytest-benchmark,
factory_boy, python-json-logger. Frontend adds Vitest, RTL, jest-dom,
@axe-core/playwright, eslint-plugin-security.

## Real-PII discipline note

This baseline file is committable: contains no client values, only
structural counts + identifiers (workspace UUIDs are opaque).
